# Compact evidence: five-replica cache-hit transport knee

This directory retains the claim-sized evidence for the 2026-09-01 Arena
cache-hit transport ladder. Full raw captures remain under the ignored local
`results/transport/` tree.

- `decision.json` contains scope, medians, transitions, and attribution.
- `SHA256SUMS` covers the tracked compact evidence.
- `decision.json` also records hashes for each ignored raw campaign summary.

The result is scoped to five W4 replicas, the unchanged classifier image,
stable 256-byte cache hits, 125 connections, and the pinned independent driver.
